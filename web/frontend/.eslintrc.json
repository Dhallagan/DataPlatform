{
  "extends": ["next/core-web-vitals"],
  "overrides": [
    {
      "files": ["src/components/ui/**/*.{ts,tsx}"],
      "rules": {
        "no-restricted-syntax": [
          "error",
          {
            "selector": "Literal[value=/#(?:[0-9a-fA-F]{3,8})/]",
            "message": "Use design tokens from tailwind.config.js instead of hardcoded hex colors."
          },
          {
            "selector": "Literal[value=/rgb\\(/]",
            "message": "Use design tokens from tailwind.config.js instead of rgb() values."
          },
          {
            "selector": "Literal[value=/hsl\\(/]",
            "message": "Use design tokens from tailwind.config.js instead of hsl() values."
          }
        ]
      }
    }
  ]
}
