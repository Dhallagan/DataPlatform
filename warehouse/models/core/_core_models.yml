version: 2

models:
  - name: organizations
    description: Canonical organization entity with subscription and usage context.
    columns:
      - name: organization_id
        tests:
          - not_null
          - unique
      - name: organization_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['active', 'suspended', 'churned']
      - name: current_plan_name
        tests:
          - accepted_values:
              arguments:
                values: ['free', 'starter', 'pro', 'enterprise']
                quote: true
              config:
                where: "current_plan_name is not null"

  - name: users
    description: Canonical user entity with organization context.
    columns:
      - name: user_id
        tests:
          - not_null
          - unique
      - name: primary_organization_id
        tests:
          - relationships:
              arguments:
                to: ref('organizations')
                field: organization_id
              config:
                where: "primary_organization_id is not null"

  - name: sessions
    description: Canonical session entity with derived metrics.
    columns:
      - name: session_id
        tests:
          - not_null
          - unique
      - name: organization_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('organizations')
                field: organization_id
      - name: session_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['pending', 'running', 'completed', 'failed', 'timeout']

  - name: dim_organizations
    description: Organization dimension for semantic joins.
    columns:
      - name: organization_id
        tests:
          - not_null
          - unique
      - name: organization_status
        tests:
          - not_null

  - name: dim_users
    description: User dimension for semantic joins.
    columns:
      - name: user_id
        tests:
          - not_null
          - unique
      - name: organization_id
        tests:
          - relationships:
              arguments:
                to: ref('dim_organizations')
                field: organization_id
              config:
                where: "organization_id is not null"

  - name: fct_runs
    description: Canonical browser run fact table.
    columns:
      - name: run_id
        tests:
          - not_null
          - unique
      - name: organization_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_organizations')
                field: organization_id
      - name: run_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['pending', 'running', 'completed', 'failed', 'timeout']

  - name: fct_events
    description: Canonical event fact table.
    columns:
      - name: event_id
        tests:
          - not_null
          - unique
      - name: run_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('fct_runs')
                field: run_id

  - name: fct_subscriptions
    description: Canonical subscription fact table.
    columns:
      - name: subscription_id
        tests:
          - not_null
          - unique
      - name: organization_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_organizations')
                field: organization_id
      - name: subscription_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['trialing', 'active', 'past_due', 'canceled']
